"""Детерминированный генератор/валидатор ходов коротких нард.

Этот модуль — **контракт API** для движка. Заполнение реальной механики
(правила коротких нард: движение, бит, вход с бара, вынос, дубли) идёт по
проверенному референсу правил (см. `docs/backgammon-rules.md`).

Ключевой инженерный принцип: **модель НЕ генерирует ходы сама**.
Она выбирает один из легальных вариантов, которые строит эта среда.
Поэтому «генерация невозможного хода» исключена на архитектурном уровне.

Нотация хода (после реш-снимка): `Move` = кортеж шагов `(from_pip, die_used)`.
"""

from __future__ import annotations

from dataclasses import dataclass, field


@dataclass(frozen=True, slots=True)
class Move:
    """Ход: последовательность перемещений из пунктов с указанием использованного кубика."""

    steps: tuple[tuple[int, int], ...] = field(default_factory=tuple)

    def __len__(self) -> int:
        return len(self.steps)

    def __repr__(self) -> str:
        return f"Move({self.steps})"


# --- TODO(reference rules): реализовать по `docs/backgammon-rules.md` ---

def legal_moves(pos, roll) -> list[Move]:
    """Все легальные полные ходы для текущего игрока при броске `roll`.

    TODO: развернуть по правилам коротких нард (референс в docs/).
    Пока заглушка — всегда пусто, чтобы невозможно было сыграть нелегально.
    """
    return []


def apply_move(pos, move: Move):
    """Применить легальный ход к позиции, вернуть новую позицию.

    TODO: реализация; пока признаст невозможным.
    """
    raise NotImplementedError("движок ходов реализуется после референса правил")